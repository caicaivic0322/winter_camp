# 📝 第7课 测试卷：插入排序

**姓名**：____________________          **得分**：____________________

---

## 一、选择题（每题5分，共30分）

**1. 插入排序的核心思想类似于（　　）**
A. 从一堆牌中选最小的　B. 整理手中的扑克牌　C. 交换相邻两张牌　D. 把牌分成两堆

**2. 插入排序的时间复杂度在最好情况下是（　　）**
A. O(1)　　B. O(n)　　C. O(n log n)　　D. O(n²)

**3. 对数组 [3, 1, 4] 进行插入排序，第一轮后结果是（　　）**
A. [1, 3, 4]　　B. [3, 1, 4]　　C. [1, 4, 3]　　D. [4, 3, 1]

**4. 插入排序是（　　）排序**
A. 不稳定　　B. 稳定　　C. 半稳定　　D. 无法确定

**5. 插入排序的空间复杂度是（　　）**
A. O(1)　　B. O(n)　　C. O(log n)　　D. O(n²)

**6. 关于插入排序的适用场景，下列说法正确的是（　　）**
A. 数据量非常大时最快
B. 数据基本有序时效率很高
C. 数据完全随机时效率最高
D. 任何情况下都比快速排序快

---

## 二、判断题（每题5分，共20分）

**1. 插入排序对基本有序的数组效率高。（　　）**

**2. 插入排序需要额外的 O(n) 空间。（　　）**

**3. 插入排序是稳定排序，即相等元素的相对位置不会改变。（　　）**

**4. 如果数组已经是有序的，插入排序的时间复杂度是 O(n^2)。（　　）**

---

## 三、程序完善题（每题25分，共50分）

### 第1题：基本插入排序

```cpp
#include <iostream>
using namespace std;

void insertionSort(int arr[], int n) {
    for (int i = 1; i < n; i++) {
        int key = ______①;
        int j = i - 1;
        // 将大于 key 的元素后移
        while (j >= 0 && arr[j] ______② key) {
            arr[j + 1] = arr[______③];
            j--;
        }
        arr[______④] = key;
    }
}

int main() {
    int arr[] = {5, 2, 4, 6, 1, 3};
    insertionSort(arr, 6);
    for(int i=0; i<6; i++) cout << arr[i] << " ";
    return 0;
}
```

### 第2题：降序插入排序

在原插入排序的基础上，修改为**降序**排列（大到小）。

```cpp
#include <iostream>
using namespace std;

void insertionSortDesc(int arr[], int n) {
    for (int i = 1; i < n; i++) {
        int key = arr[i];
        int j = i - 1;
        // 修改循环条件实现降序
        while (j >= 0 && arr[j] ______① key) {
            arr[j + 1] = arr[j];
            j--;
        }
        arr[j + 1] = ______②;
    }
}

int main() {
    int arr[] = {1, 2, 3, 4, 5};
    insertionSortDesc(arr, 5);
    // 应输出 5 4 3 2 1
    for(int i=0; i<5; i++) cout << arr[i] << " ";
    return 0;
}
```

---
