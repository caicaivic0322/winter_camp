System.register([],function(n,e){"use strict";return{execute:function(){n("default",'# 📘 第9课：树与二叉树\n\n> **课程定位**：核心数据结构 —— 掌握递归与树的遍历思维\n\n---\n\n## 一、学习目标\n\n通过本课学习，你将能够：\n\n1. ✅ 理解树的基本概念和术语\n2. ✅ 掌握二叉树的性质和特点\n3. ✅ 学会用数组和指针表示二叉树\n4. ✅ 掌握二叉树的三种遍历方式\n5. ✅ 理解递归在树结构中的应用\n\n---\n\n## 二、为什么学习树？\n\n### 2.1 树的思想无处不在\n\n- **文件系统**：目录和文件的层级结构\n- **HTML/XML**：标签的嵌套关系\n- **组织架构**：公司的层级管理\n- **分治算法**：归并排序、快速排序的递归过程\n\n### 2.2 树的思维方式\n\n学习树不仅是学习新的数据结构，更是为了掌握**递归**和**分治**的思维方式。后续学习的许多复杂算法，本质上都是在对树进行遍历。\n\n---\n\n## 三、树的基本概念\n\n### 3.1 什么是树？\n\n**树**是由 n（n≥0）个节点组成的有限集合，满足：\n\n- 有且仅有一个**根节点**\n- 其余节点分成若干个**子树**\n\n### 3.2 树的术语\n\n```\n        A          ← 根节点（Root）\n       /|\\\n      B C D        ← A的子节点\n     /|   |\n    E F   G        ← 叶子节点（没有子节点）\n```\n\n| 术语     | 说明                         | 示例            |\n| -------- | ---------------------------- | --------------- |\n| 根节点   | 没有父节点的节点             | A               |\n| 父节点   | 某节点的上一层节点           | B 是 E 的父节点 |\n| 子节点   | 某节点的下一层节点           | E 是 B 的子节点 |\n| 兄弟节点 | 同一父节点的节点             | E 和 F 是兄弟   |\n| 叶子节点 | 没有子节点的节点             | E, F, C, G      |\n| 深度     | 从根到该节点的路径长度       | B 的深度是 1    |\n| 高度     | 从该节点到最远叶子的路径长度 | A 的高度是 2    |\n\n### 3.3 树的高度和深度\n\n```\n深度 0:      A\n            / \\\n深度 1:    B   C\n          / \\\n深度 2:  D   E\n\n高度 = 最大深度 = 2\n```\n\n---\n\n## 四、二叉树\n\n### 4.1 什么是二叉树？\n\n**二叉树**：每个节点最多有 **2 个子节点**（左子节点和右子节点）\n\n```\n      1\n     / \\\n    2   3\n   / \\   \\\n  4   5   6\n```\n\n### 4.2 二叉树的特点\n\n- 每个节点最多有 2 个子节点\n- 左子树和右子树是有区别的（有序的）\n- 即使只有一个子节点，也要区分是左还是右\n\n### 4.3 特殊的二叉树\n\n**满二叉树**：每一层都是满的\n\n```\n       1\n      / \\\n     2   3\n    /|   |\\\n   4 5   6 7\n```\n\n**完全二叉树**：除最后一层外，其他层都是满的，最后一层的节点从左到右连续\n\n```\n       1\n      / \\\n     2   3\n    / \\  /\n   4  5 6\n```\n\n---\n\n## 五、二叉树的存储\n\n### 5.1 数组存储（适合完全二叉树）\n\n对于编号从 1 开始的完全二叉树，设父节点索引为 `i`：\n\n- 左子节点：`i * 2`\n- 右子节点：`i * 2 + 1`\n\n```cpp\n//        1\n//       / \\\n//      2   3\n//     / \\\n//    4   5\n\nint tree[10] = {0, 1, 2, 3, 4, 5};  // 下标从1开始\n// tree[1] = 1 (根)\n// tree[2] = 2 (1的左子)\n// tree[3] = 3 (1的右子)\n// tree[4] = 4 (2的左子)\n// tree[5] = 5 (2的右子)\n```\n\n### 5.2 链式存储（适合所有二叉树）\n\n```cpp\nstruct TreeNode {\n    int val;           // 节点值\n    TreeNode* left;    // 左子节点\n    TreeNode* right;   // 右子节点\n\n    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}\n};\n```\n\n### 5.3 创建二叉树示例\n\n```cpp\n#include <iostream>\nusing namespace std;\n\nstruct TreeNode {\n    int val;\n    TreeNode* left;\n    TreeNode* right;\n    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}\n};\n\nint main() {\n    // 创建二叉树:\n    //       1\n    //      / \\\n    //     2   3\n    //    / \\\n    //   4   5\n\n    TreeNode* root = new TreeNode(1);\n    root->left = new TreeNode(2);\n    root->right = new TreeNode(3);\n    root->left->left = new TreeNode(4);\n    root->left->right = new TreeNode(5);\n\n    cout << "根节点: " << root->val << endl;\n    cout << "左子节点: " << root->left->val << endl;\n    cout << "右子节点: " << root->right->val << endl;\n\n    return 0;\n}\n```\n\n---\n\n## 六、⭐ 二叉树的遍历\n\n### 6.1 三种遍历方式\n\n| 遍历方式 | 顺序     | 记忆口诀 |\n| -------- | -------- | -------- |\n| 前序遍历 | 根→左→右 | 根在前   |\n| 中序遍历 | 左→根→右 | 根在中   |\n| 后序遍历 | 左→右→根 | 根在后   |\n\n### 6.2 遍历示例\n\n```\n       1\n      / \\\n     2   3\n    / \\\n   4   5\n\n前序遍历: 1, 2, 4, 5, 3  （先访问根，再左子树，再右子树）\n中序遍历: 4, 2, 5, 1, 3  （先左子树，再根，再右子树）\n后序遍历: 4, 5, 2, 3, 1  （先左子树，再右子树，再根）\n```\n\n### 6.3 递归实现\n\n```cpp\n#include <iostream>\nusing namespace std;\n\nstruct TreeNode {\n    int val;\n    TreeNode* left;\n    TreeNode* right;\n    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}\n};\n\n// 前序遍历：根 → 左 → 右\nvoid preorder(TreeNode* root) {\n    if (root == nullptr) return;\n\n    cout << root->val << " ";  // 访问根\n    preorder(root->left);       // 遍历左子树\n    preorder(root->right);      // 遍历右子树\n}\n\n// 中序遍历：左 → 根 → 右\nvoid inorder(TreeNode* root) {\n    if (root == nullptr) return;\n\n    inorder(root->left);        // 遍历左子树\n    cout << root->val << " ";  // 访问根\n    inorder(root->right);       // 遍历右子树\n}\n\n// 后序遍历：左 → 右 → 根\nvoid postorder(TreeNode* root) {\n    if (root == nullptr) return;\n\n    postorder(root->left);      // 遍历左子树\n    postorder(root->right);     // 遍历右子树\n    cout << root->val << " ";  // 访问根\n}\n\nint main() {\n    // 创建二叉树\n    TreeNode* root = new TreeNode(1);\n    root->left = new TreeNode(2);\n    root->right = new TreeNode(3);\n    root->left->left = new TreeNode(4);\n    root->left->right = new TreeNode(5);\n\n    cout << "前序遍历: ";\n    preorder(root);\n    cout << endl;\n\n    cout << "中序遍历: ";\n    inorder(root);\n    cout << endl;\n\n    cout << "后序遍历: ";\n    postorder(root);\n    cout << endl;\n\n    return 0;\n}\n```\n\n**输出**：\n\n```\n前序遍历: 1 2 4 5 3\n中序遍历: 4 2 5 1 3\n后序遍历: 4 5 2 3 1\n```\n\n### 6.4 遍历的理解技巧\n\n**画圈法**：\n\n1. 从根节点出发，沿着树的外沿画一圈\n2. 前序：第一次经过节点左侧时访问\n3. 中序：第一次经过节点下方时访问\n4. 后序：第一次经过节点右侧时访问\n\n---\n\n## 七、二叉树的常见操作\n\n### 7.1 计算节点数\n\n```cpp\nint countNodes(TreeNode* root) {\n    if (root == nullptr) return 0;\n    return 1 + countNodes(root->left) + countNodes(root->right);\n}\n```\n\n### 7.2 计算高度\n\n```cpp\nint getHeight(TreeNode* root) {\n    if (root == nullptr) return 0;\n    return 1 + max(getHeight(root->left), getHeight(root->right));\n}\n```\n\n### 7.3 查找节点\n\n```cpp\nTreeNode* search(TreeNode* root, int target) {\n    if (root == nullptr) return nullptr;\n    if (root->val == target) return root;\n\n    TreeNode* leftResult = search(root->left, target);\n    if (leftResult != nullptr) return leftResult;\n\n    return search(root->right, target);\n}\n```\n\n### 7.4 统计叶子节点\n\n```cpp\nint countLeaves(TreeNode* root) {\n    if (root == nullptr) return 0;\n    if (root->left == nullptr && root->right == nullptr) {\n        return 1;  // 是叶子节点\n    }\n    return countLeaves(root->left) + countLeaves(root->right);\n}\n```\n\n---\n\n## 八、经典递归代码练习\n\n通过以下四个经典练习，深入体会“递归”与“树”的紧密联系。\n\n### 8.1 练习一：斐波那契数列（递归树）\n\n计算 `fib(n)` 的过程本质上是在构建一棵二叉树。\n\n```cpp\nint fib(int n) {\n    if (n <= 1) return n;\n    // 每次调用分裂成两个子任务，就像树的分叉\n    return fib(n - 1) + fib(n - 2);\n}\n```\n\n### 8.2 练习二：汉诺塔问题 (Hanoi)\n\n经典的递归问题。虽然不是直接操作二叉树，但其函数调用过程是一棵完美的二叉树。\n\n```cpp\n// 将 n 个盘子从 A 柱移到 C 柱，利用 B 柱\nvoid hanoi(int n, char A, char B, char C) {\n    if (n == 1) {\n        cout << "Move disk 1 from " << A << " to " << C << endl;\n        return;\n    }\n    hanoi(n - 1, A, C, B); // 将 n-1 个盘子从 A 移到 B\n    cout << "Move disk " << n << " from " << A << " to " << C << endl;\n    hanoi(n - 1, B, A, C); // 将 n-1 个盘子从 B 移到 C\n}\n```\n\n### 8.3 练习三：翻转二叉树\n\n将二叉树的所有左右子树交换（镜像翻转）。\n\n```cpp\nvoid invertTree(TreeNode* root) {\n    if (root == nullptr) return;\n\n    // 1. 交换当前节点的左右孩子\n    swap(root->left, root->right);\n\n    // 2. 递归翻转左子树\n    invertTree(root->left);\n\n    // 3. 递归翻转右子树\n    invertTree(root->right);\n}\n```\n\n### 8.4 练习四：判断两棵树是否相同\n\n递归比较两棵树的结构和值是否完全一致。\n\n```cpp\nbool isSameTree(TreeNode* p, TreeNode* q) {\n    // 1. 都为空，相同\n    if (p == nullptr && q == nullptr) return true;\n\n    // 2. 一个为空一个不空，不同\n    if (p == nullptr || q == nullptr) return false;\n\n    // 3. 值不同，不同\n    if (p->val != q->val) return false;\n\n    // 4. 递归比较左右子树\n    return isSameTree(p->left, q->left) &&\n           isSameTree(p->right, q->right);\n}\n```\n\n---\n\n## 九、用数组模拟二叉树\n\n对于完全二叉树，用数组存储非常高效：\n\n```cpp\n#include <iostream>\nusing namespace std;\n\nclass ArrayTree {\nprivate:\n    int tree[100];\n    int size;\n\npublic:\n    ArrayTree() : size(0) {\n        fill(tree, tree + 100, 0);\n    }\n\n    // 添加节点\n    void add(int val) {\n        tree[++size] = val;\n    }\n\n    // 获取父节点\n    int parent(int i) {\n        return tree[i / 2];\n    }\n\n    // 获取左子节点\n    int left(int i) {\n        return 2 * i <= size ? tree[2 * i] : -1;\n    }\n\n    // 获取右子节点\n    int right(int i) {\n        return 2 * i + 1 <= size ? tree[2 * i + 1] : -1;\n    }\n\n    // 前序遍历\n    void preorder(int i) {\n        if (i > size || tree[i] == 0) return;\n        cout << tree[i] << " ";\n        preorder(2 * i);\n        preorder(2 * i + 1);\n    }\n\n    // 中序遍历\n    void inorder(int i) {\n        if (i > size || tree[i] == 0) return;\n        inorder(2 * i);\n        cout << tree[i] << " ";\n        inorder(2 * i + 1);\n    }\n};\n\nint main() {\n    ArrayTree tree;\n    tree.add(1);\n    tree.add(2);\n    tree.add(3);\n    tree.add(4);\n    tree.add(5);\n\n    cout << "前序遍历: ";\n    tree.preorder(1);\n    cout << endl;\n\n    cout << "中序遍历: ";\n    tree.inorder(1);\n    cout << endl;\n\n    return 0;\n}\n```\n\n---\n\n## 十、本课小结\n\n### 核心概念\n\n| 概念   | 说明                   |\n| ------ | ---------------------- |\n| 树     | 层次结构的数据组织方式 |\n| 二叉树 | 每个节点最多两个子节点 |\n| 遍历   | 按特定顺序访问所有节点 |\n| 递归   | 树的自然处理方式       |\n\n### 三种遍历\n\n| 遍历 | 顺序     | 应用场景         |\n| ---- | -------- | ---------------- |\n| 前序 | 根→左→右 | 复制树、快速排序 |\n| 中序 | 左→根→右 | 二叉搜索树排序   |\n| 后序 | 左→右→根 | 删除树、归并排序 |\n\n1. 如何不用递归实现二叉树遍历？（提示：用栈）\n2. 给定前序和中序遍历，能否还原二叉树？\n3. 尝试手画 3 层的汉诺塔的递归调用树。\n\n---\n\n**下节预告**：第10课 - 归并排序，分治思想的实战应用！\n')}}});
