System.register([],function(n,t){"use strict";return{execute:function(){n("default",'# 📘 第5章 条件分支\n\n> 目标：掌握逻辑运算与多层分支的写法，能解决多条件分类问题\n\n---\n\n## 5.1 逻辑运算符\n- 与：`&&`；或：`||`；非：`!`（存在短路，前半已能确定结果时不再计算后半）\n```cpp\nbool a = true, b = false;\ncout << (a && b) << " " << (a || b) << " " << (!a) << endl;\n```\n\n---\n\n## 5.2 分支嵌套\n```cpp\nint t; cin >> t;\nif (t >= 0) {\n    if (t <= 35) cout << "normal\\n";\n    else cout << "hot\\n";\n} else {\n    cout << "cold\\n";\n}\n```\n\n---\n\n## 5.3 条件分支的综合运用\n问题：读取一个日期（年、月、日），判断其是否合法。\n```cpp\n#include <iostream>\nusing namespace std;\n\nbool isLeap(int y) { return (y % 400 == 0) || (y % 4 == 0 && y % 100 != 0); }\n\nint main() {\n    int y, m, d; cin >> y >> m >> d;\n    int md[] = {0,31,28,31,30,31,30,31,31,30,31,30,31};\n    if (m == 2 && isLeap(y)) md[2] = 29;\n    if (y >= 1 && m >= 1 && m <= 12 && d >= 1 && d <= md[m]) cout << "valid\\n";\n    else cout << "invalid\\n";\n    return 0;\n}\n```\n\n---\n\n## 小练习\n1) 读入三条边长，判断能否构成三角形，若能则判断是否直角三角形（勾股定理）\n2) 根据分数输出等级：90+ A，80-89 B，60-79 C，其他 D（使用多重 if-else）\n')}}});
