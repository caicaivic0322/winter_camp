System.register([],function(n,i){"use strict";return{execute:function(){n("default",'# 📘 第12课：动态规划基础\n\n> **课程定位**：算法皇冠上的明珠 —— 理解状态转移与最优子结构\n\n---\n\n## 一、学习目标\n\n1. ✅ 理解动态规划（DP）的核心思想\n2. ✅ 掌握"状态"与"状态转移方程"\n3. ✅ 能够解决基础 DP 问题（爬楼梯、斐波那契）\n4. ✅ 了解记忆化搜索与递推的区别\n\n---\n\n## 二、什么是动态规划？\n\n### 2.1 核心概念\n\n动态规划（Dynamic Programming，简称 DP）通过把原问题分解为相对简单的子问题的方式求解复杂问题。\n\n**关键特征**：\n1. **重叠子问题**：子问题会被重复计算（如斐波那契数列）。\n2. **最优子结构**：问题的最优解包含其子问题的最优解。\n3. **无后效性**：一旦某个状态确定，就不会受未来决策的影响。\n\n### 2.2 入门案例：斐波那契数列\n\n求第 N 项斐波那契数：1, 1, 2, 3, 5, 8...\n\n#### 方法1：递归（暴力）\n```cpp\nint fib(int n) {\n    if (n <= 2) return 1;\n    return fib(n-1) + fib(n-2);\n}\n// 缺点：大量重复计算，O(2^n)\n```\n\n#### 方法2：记忆化搜索（自顶向下）\n```cpp\nint memo[1000];\nint fib(int n) {\n    if (n <= 2) return 1;\n    if (memo[n] != 0) return memo[n]; // 查表\n    return memo[n] = fib(n-1) + fib(n-2);\n}\n// 优化：O(n)\n```\n\n#### 方法3：动态规划（自底向上）\n```cpp\nint dp[1000];\ndp[1] = dp[2] = 1;\nfor (int i = 3; i <= n; i++) {\n    dp[i] = dp[i-1] + dp[i-2];\n}\n// 优化：O(n)\n```\n\n---\n\n## 三、经典问题：爬楼梯\n\n**问题**：假设你正在爬楼梯。需要 n 阶你才能到达楼顶。每次你可以爬 1 或 2 个台阶。你有多少种不同的方法可以爬到楼顶呢？\n\n### 3.1 分析\n\n- 到达第 i 阶，只能从第 i-1 阶跳 1 步，或者从第 i-2 阶跳 2 步而来。\n- 所以：`dp[i] = dp[i-1] + dp[i-2]`。\n- 初始状态：`dp[1] = 1`, `dp[2] = 2`。\n\n### 3.2 代码实现\n\n```cpp\n#include <iostream>\nusing namespace std;\n\nint main() {\n    int n;\n    cin >> n;\n    \n    if (n == 1) { cout << 1; return 0; }\n    \n    int dp[100];\n    dp[1] = 1;\n    dp[2] = 2;\n    \n    for (int i = 3; i <= n; i++) {\n        dp[i] = dp[i-1] + dp[i-2];\n    }\n    \n    cout << dp[n] << endl;\n    return 0;\n}\n```\n\n---\n\n## 四、经典问题：0/1 背包问题（简介）\n\n**问题**：有 N 件物品和一个容量为 V 的背包。第 i 件物品的体积是 c[i]，价值是 w[i]。每件物品只能用一次，求最大价值。\n\n### 4.1 状态定义\n\n`dp[i][j]` 表示前 i 件物品放入容量为 j 的背包可以获得的最大价值。\n\n### 4.2 状态转移方程\n\n对于第 i 件物品，我们有两种选择：\n1. **不放**：`dp[i][j] = dp[i-1][j]`\n2. **放**：`dp[i][j] = dp[i-1][j-c[i]] + w[i]` （前提是能放得下，j >= c[i]）\n\n综合：\n`dp[i][j] = max(dp[i-1][j], dp[i-1][j-c[i]] + w[i])`\n\n---\n\n## 五、随堂练习\n\n### 练习 1：最小花费爬楼梯\n\n**题目**：给你一个整数数组 `cost`，其中 `cost[i]` 是从楼梯第 `i` 个台阶向上爬需要支付的费用。一旦你支付此费用，即可选择向上爬一个或者两个台阶。你可以选择从下标为 0 或下标为 1 的台阶开始爬楼梯。请你计算并返回达到楼梯顶部的最低花费。\n\n**提示**：\n`dp[i]` 表示到达第 i 阶的最小花费。\n`dp[i] = min(dp[i-1] + cost[i-1], dp[i-2] + cost[i-2])`\n\n### 练习 2：最大子数组和\n\n**题目**：给你一个整数数组 `nums`，请你找出一个具有最大和的连续子数组（子数组最少包含一个元素），返回其最大和。\n\n**提示**：\n`dp[i]` 表示以 `nums[i]` 结尾的最大子数组和。\n`dp[i] = max(nums[i], dp[i-1] + nums[i])`\n如果 `dp[i-1] < 0`，那么加上它不如不加，直接从 `nums[i]` 开始。\n\n```cpp\nint maxSubArray(int nums[], int n) {\n    int dp[100]; // 假设 n < 100\n    dp[0] = nums[0];\n    int maxRes = dp[0];\n    for (int i = 1; i < n; i++) {\n        dp[i] = max(nums[i], dp[i-1] + nums[i]);\n        maxRes = max(maxRes, dp[i]);\n    }\n    return maxRes;\n}\n```\n\n---\n\n## 六、本课小结\n\n1. **DP 三步走**：\n   - 定义状态（dp 数组的含义）\n   - 寻找状态转移方程（核心逻辑）\n   - 确定初始条件和边界\n2. **思想**：用空间换时间，避免重复计算。\n\n---\n\n**下节预告**：第13课 - 堆与优先队列，高效处理最值问题！\n')}}});
