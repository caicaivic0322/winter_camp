System.register([],function(n,i){"use strict";return{execute:function(){n("default",'# 📘 第13课：堆与堆排序\n\n> **课程定位**：偏应用 —— 理解堆的结构，会用 priority_queue\n\n---\n\n## 一、学习目标\n\n1. ✅ 理解堆的概念和性质\n2. ✅ 掌握用数组模拟堆\n3. ✅ 了解堆排序的基本原理\n4. ✅ 熟练使用 `priority_queue`\n\n---\n\n## 二、堆的概念\n\n### 2.1 什么是堆？\n\n**堆**是一种特殊的完全二叉树，满足：\n\n- **大顶堆**：每个节点的值 ≥ 子节点的值\n- **小顶堆**：每个节点的值 ≤ 子节点的值\n\n### 2.2 堆的性质\n\n```\n大顶堆示例：\n        9\n       / \\\n      7   8\n     / \\   \\\n    5   6   3\n\n数组表示：[9, 7, 8, 5, 6, 3]\n下标：     0  1  2  3  4  5\n```\n\n**关键关系**（从 0 开始编号）：\n\n- 父节点：`(i - 1) / 2`\n- 左子节点：`2 * i + 1`\n- 右子节点：`2 * i + 2`\n\n---\n\n## 三、堆的基本操作\n\n### 3.1 上浮（插入新元素）\n\n```cpp\nvoid siftUp(int arr[], int i) {\n    while (i > 0 && arr[i] > arr[(i - 1) / 2]) {\n        swap(arr[i], arr[(i - 1) / 2]);\n        i = (i - 1) / 2;\n    }\n}\n```\n\n### 3.2 下沉（删除堆顶）\n\n```cpp\nvoid siftDown(int arr[], int i, int n) {\n    while (2 * i + 1 < n) {\n        int j = 2 * i + 1;  // 左子节点\n        if (j + 1 < n && arr[j + 1] > arr[j]) j++;  // 选更大的子节点\n        if (arr[i] >= arr[j]) break;\n        swap(arr[i], arr[j]);\n        i = j;\n    }\n}\n```\n\n### 3.3 建堆\n\n```cpp\nvoid buildHeap(int arr[], int n) {\n    // 从最后一个非叶节点开始下沉\n    for (int i = n / 2 - 1; i >= 0; i--) {\n        siftDown(arr, i, n);\n    }\n}\n```\n\n---\n\n## 四、堆排序\n\n### 4.1 原理\n\n1. 建立大顶堆\n2. 将堆顶（最大值）与末尾交换\n3. 堆大小减 1，对堆顶进行下沉\n4. 重复步骤 2-3\n\n### 4.2 代码实现\n\n```cpp\n#include <iostream>\nusing namespace std;\n\nvoid siftDown(int arr[], int i, int n) {\n    while (2 * i + 1 < n) {\n        int j = 2 * i + 1;\n        if (j + 1 < n && arr[j + 1] > arr[j]) j++;\n        if (arr[i] >= arr[j]) break;\n        swap(arr[i], arr[j]);\n        i = j;\n    }\n}\n\nvoid heapSort(int arr[], int n) {\n    // 建堆\n    for (int i = n / 2 - 1; i >= 0; i--) {\n        siftDown(arr, i, n);\n    }\n\n    // 排序\n    for (int i = n - 1; i > 0; i--) {\n        swap(arr[0], arr[i]);  // 堆顶与末尾交换\n        siftDown(arr, 0, i);   // 下沉\n    }\n}\n\nint main() {\n    int arr[] = {5, 3, 8, 4, 2, 7, 1, 6};\n    int n = 8;\n    heapSort(arr, n);\n    for (int i = 0; i < n; i++) cout << arr[i] << " ";\n    return 0;\n}\n```\n\n---\n\n## 五、priority_queue 的使用\n\n### 5.1 基本用法\n\n```cpp\n#include <iostream>\n#include <queue>\nusing namespace std;\n\nint main() {\n    // 大顶堆（默认）\n    priority_queue<int> maxHeap;\n    maxHeap.push(3);\n    maxHeap.push(1);\n    maxHeap.push(4);\n    cout << maxHeap.top() << endl;  // 4\n    maxHeap.pop();\n    cout << maxHeap.top() << endl;  // 3\n\n    // 小顶堆\n    priority_queue<int, vector<int>, greater<int>> minHeap;\n    minHeap.push(3);\n    minHeap.push(1);\n    minHeap.push(4);\n    cout << minHeap.top() << endl;  // 1\n\n    return 0;\n}\n```\n\n### 5.2 常用操作\n\n| 操作   | 方法      | 时间复杂度 |\n| ------ | --------- | ---------- |\n| 入堆   | `push(x)` | O(log n)   |\n| 取堆顶 | `top()`   | O(1)       |\n| 出堆   | `pop()`   | O(log n)   |\n| 判空   | `empty()` | O(1)       |\n| 大小   | `size()`  | O(1)       |\n\n---\n\n## 六、应用：Top K 问题\n\n### 找最大的 K 个数\n\n```cpp\n#include <iostream>\n#include <queue>\nusing namespace std;\n\nint main() {\n    int arr[] = {3, 2, 1, 5, 6, 4};\n    int n = 6, k = 2;\n\n    // 使用小顶堆，维护 k 个最大的数\n    priority_queue<int, vector<int>, greater<int>> minHeap;\n\n    for (int i = 0; i < n; i++) {\n        minHeap.push(arr[i]);\n        if (minHeap.size() > k) minHeap.pop();\n    }\n\n    // 堆中就是最大的 k 个数\n    while (!minHeap.empty()) {\n        cout << minHeap.top() << " ";\n        minHeap.pop();\n    }\n    // 输出: 5 6\n\n    return 0;\n}\n```\n\n---\n\n## 七、时间复杂度\n\n| 操作          | 时间复杂度 |\n| ------------- | ---------- |\n| 建堆          | O(n)       |\n| 堆排序        | O(n log n) |\n| 单次插入/删除 | O(log n)   |\n\n---\n\n## 七、随堂练习\n\n### 练习 1：最后一块石头的重量\n\n**题目**：有一堆石头，每块石头的重量都是正整数。每一回合，从中选出两块**最重**的石头，然后将它们一起粉碎。假设石头的重量分别为 `x` 和 `y`，且 `x <= y`。那么粉碎的可能结果如下：\n- 如果 `x == y`，那么两块石头都会被完全粉碎；\n- 如果 `x != y`，那么重量为 `x` 的石头被完全粉碎，而重量为 `y` 的石头新重量为 `y-x`。\n最后，最多只会剩下一块石头。返回此石头的重量。如果没有石头剩下，就返回 0。\n\n**思路**：使用大顶堆，每次取出两个最大的，处理后如果还有剩余则放回堆中。\n\n```cpp\n// 核心逻辑\npriority_queue<int> pq;\nfor (int w : stones) pq.push(w);\n\nwhile (pq.size() > 1) {\n    int y = pq.top(); pq.pop();\n    int x = pq.top(); pq.pop();\n    if (x != y) pq.push(y - x);\n}\nreturn pq.empty() ? 0 : pq.top();\n```\n\n### 练习 2：合并 K 个有序链表（思路）\n\n**题目**：给你 K 个升序链表，将它们合并成一个有序链表。\n\n**思路**：\n1. 将每个链表的头节点放入小顶堆中。\n2. 每次取出堆顶（最小值），加入结果链表。\n3. 如果取出的节点有下一个节点，将其放入堆中。\n4. 重复直到堆为空。\n\n---\n\n## 八、本课小结\n\n### 核心知识点\n\n1. 堆是完全二叉树，用数组存储\n2. 父子关系通过下标计算\n3. `priority_queue` 是 STL 提供的堆实现\n4. 堆排序时间复杂度 O(n log n)，空间 O(1)\n\n### 实用技巧\n\n- 大顶堆：`priority_queue<int>`\n- 小顶堆：`priority_queue<int, vector<int>, greater<int>>`\n- Top K 问题用堆高效解决\n\n---\n\n**下节预告**：第14课 - 搜索算法，广度优先与深度优先搜索！\n')}}});
