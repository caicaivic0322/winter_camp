const n='# 📘 第10课：分治算法与归并排序\n\n> **课程定位**：分而治之 —— 从二分查找到归并排序\n\n---\n\n## 一、学习目标\n\n1. ✅ 理解**分治算法**的核心思想（Divide and Conquer）\n2. ✅ 掌握**二分查找**及其变种（`lower_bound`/`upper_bound`）\n3. ✅ 掌握**归并排序**的原理与实现\n4. ✅ 学会利用归并排序求**逆序对**\n\n---\n\n## 二、分治思想\n\n### 2.1 核心概念\n\n分治法将一个难以直接解决的大问题，分割成一些规模较小的相同问题，以便各个击破，分而治之。\n\n**三个步骤**：\n1. **分解（Divide）**：将原问题分解为若干个规模较小，相互独立，与原问题形式相同的子问题。\n2. **解决（Conquer）**：若子问题规模较小而容易被解决则直接解，否则递归地解各个子问题。\n3. **合并（Combine）**：将各个子问题的解合并为原问题的解。\n\n---\n\n## 三、二分查找 (Binary Search)\n\n### 3.1 算法原理\n\n在**有序数组**中查找特定元素。每次比较中间元素，将搜索范围缩小一半。\n\n### 3.2 代码实现\n\n```cpp\n// 查找 target 是否存在\nbool binarySearch(int arr[], int n, int target) {\n    int left = 0, right = n - 1;\n    while (left <= right) {\n        int mid = left + (right - left) / 2;\n        if (arr[mid] == target) return true;\n        else if (arr[mid] < target) left = mid + 1;\n        else right = mid - 1;\n    }\n    return false;\n}\n```\n\n### 3.3 STL 中的二分\n\n- `binary_search`: 检查是否存在\n- `lower_bound`: 找第一个 **≥** target 的位置\n- `upper_bound`: 找第一个 **>** target 的位置\n\n```cpp\nint arr[] = {1, 2, 4, 4, 6};\nint pos1 = lower_bound(arr, arr+5, 4) - arr; // 返回 2 (第一个4的下标)\nint pos2 = upper_bound(arr, arr+5, 4) - arr; // 返回 4 (6的下标)\n```\n\n---\n\n## 四、归并排序 (Merge Sort)\n\n### 4.1 算法流程\n\n1. **分解**：将数组从中点分开，递归排序左右两半。\n2. **合并**：将两个有序的子数组合并成一个有序数组。\n\n### 4.2 代码实现\n\n```cpp\nint temp[100005]; // 辅助数组\n\nvoid merge(int arr[], int l, int mid, int r) {\n    int i = l, j = mid + 1, k = l;\n    while (i <= mid && j <= r) {\n        if (arr[i] <= arr[j]) temp[k++] = arr[i++];\n        else temp[k++] = arr[j++];\n    }\n    while (i <= mid) temp[k++] = arr[i++];\n    while (j <= r) temp[k++] = arr[j++];\n    for (int i = l; i <= r; i++) arr[i] = temp[i];\n}\n\nvoid mergeSort(int arr[], int l, int r) {\n    if (l >= r) return;\n    int mid = (l + r) / 2;\n    mergeSort(arr, l, mid);\n    mergeSort(arr, mid + 1, r);\n    merge(arr, l, mid, r);\n}\n```\n\n### 4.3 复杂度\n\n- 时间复杂度：$O(n \\log n)$\n- 空间复杂度：$O(n)$（需要辅助数组）\n- 稳定性：**稳定**\n\n---\n\n## 五、进阶应用：逆序对\n\n### 5.1 什么是逆序对？\n\n在数组中，如果 `i < j` 且 `arr[i] > arr[j]`，则 `(i, j)` 就是一个逆序对。\n例如：`[2, 4, 3, 5, 1]` 的逆序对有 `(2,1), (4,3), (4,1), (3,1), (5,1)` 共 5 个。\n\n### 5.2 利用归并排序统计\n\n在 `merge` 过程中，当右边元素 `arr[j]` 放入 `temp` 时，说明左边剩下的所有元素（从 `i` 到 `mid`）都比 `arr[j]` 大。\n\n```cpp\nlong long cnt = 0; // 逆序对可能很多，用 long long\n\nvoid merge(int arr[], int l, int mid, int r) {\n    int i = l, j = mid + 1, k = l;\n    while (i <= mid && j <= r) {\n        if (arr[i] <= arr[j]) {\n            temp[k++] = arr[i++];\n        } else {\n            temp[k++] = arr[j++];\n            cnt += (mid - i + 1); // 核心：累加逆序对\n        }\n    }\n    // ...其余部分同普通归并\n    while (i <= mid) temp[k++] = arr[i++];\n    while (j <= r) temp[k++] = arr[j++];\n    for (int i = l; i <= r; i++) arr[i] = temp[i];\n}\n```\n\n---\n\n## 六、随堂练习\n\n### 练习 1：查找第一个出现的位置\n\n**题目**：给定一个升序数组（可能包含重复元素）和一个目标值 target，请找出 target 第一次出现的下标。如果不存在，输出 -1。\n\n```cpp\n#include <iostream>\n#include <algorithm>\nusing namespace std;\n\nint main() {\n    int arr[] = {1, 2, 4, 4, 4, 6};\n    int n = 6;\n    int target = 4;\n    \n    // 使用 lower_bound\n    int pos = lower_bound(arr, arr + n, target) - arr;\n    \n    if (pos < n && arr[pos] == target) {\n        cout << "First occurrence: " << pos << endl;\n    } else {\n        cout << "-1" << endl;\n    }\n    return 0;\n}\n```\n\n### 练习 2：逆序对计数\n\n**题目**：使用归并排序求数组 `[5, 4, 2, 6, 3, 1]` 的逆序对数量。\n\n```cpp\n// 核心代码片段\n// 在 merge 函数中：\n// if (arr[i] <= arr[j]) ...\n// else {\n//     temp[k++] = arr[j++];\n//     cnt += (mid - i + 1); \n// }\n// 最终结果应为 11\n```\n\n---\n\n## 七、本课小结\n\n1. **分治**是算法设计的重要思想：分而治之。\n2. **二分查找**是 $O(\\log n)$ 的高效搜索工具。\n3. **归并排序**不仅能排序，还能解决**逆序对**等统计问题。\n\n---\n\n**下节预告**：第11课 - 快速排序与贪心，另一种分治策略！\n';export{n as default};
