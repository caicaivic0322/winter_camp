System.register([],function(n,t){"use strict";return{execute:function(){n("default",'# 📘 第13章 动态数组\n\n> 目标：熟练使用 `vector` 与 `string`，理解动态与静态数组的差异\n\n---\n\n## 13.1 vector 容器\n```cpp\n#include <iostream>\n#include <vector>\nusing namespace std;\n\nint main() {\n    vector<int> v = {3,1,4};\n    v.push_back(1);\n    sort(v.begin(), v.end());\n    for (int x : v) cout << x << " ";\n    cout << "\\nsize=" << v.size() << endl;\n    return 0;\n}\n```\n- 动态扩容、`push_back`、迭代器 `begin()/end()`、`size()`\n\n---\n\n## 13.2 string 类\n```cpp\nstring s = "hello";\ns += " world";\nauto pos = s.find("lo");      // 返回下标或 string::npos\nif (pos != string::npos) s.replace(pos, 2, "LO");\n```\n\n---\n\n## 13.3 动态数组与静态数组的对比\n- 静态数组：大小固定、栈上分配；`int a[100]`\n- 动态数组：可变长度、堆上分配；`vector<int> v`\n- 竞赛中优先使用 `vector`，更灵活安全\n\n---\n\n## 小练习\n1) 读入 n 个整数，删除所有等于 x 的元素（保持相对顺序），输出结果\n2) 合并两个升序 `vector<int>` 并输出升序结果\n')}}});
