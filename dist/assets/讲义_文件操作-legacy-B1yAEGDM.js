System.register([],function(n,t){"use strict";return{execute:function(){n("default",'# 📘 第16章 文件操作\n\n> 目标：学会用 C++ 读写文本文件，完成批量数据处理\n\n---\n\n## 16.1 基本打开与关闭\n```cpp\n#include <fstream>\nusing namespace std;\n\nint main() {\n    ifstream fin("input.txt");\n    ofstream fout("output.txt");\n    if (!fin.is_open() || !fout.is_open()) return 0;\n    // ...\n    fin.close(); fout.close();\n}\n```\n\n---\n\n## 16.2 文件读写\n```cpp\nint x, y;\nwhile (fin >> x >> y) {  // 按空白分隔读取直到 EOF\n    fout << (x + y) << "\\n";\n}\n```\n\n---\n\n## 16.3 行读取与字符串处理\n```cpp\n#include <string>\nstring line;\nwhile (getline(fin, line)) {\n    // 处理整行\n    fout << line << "\\n";\n}\n```\n\n---\n\n## 小练习\n1) 从 `data.txt` 读取若干行，每行是若干整数，输出每行的和到 `sum.txt`\n2) 读取文本统计单词总数（按空白切分）\n')}}});
