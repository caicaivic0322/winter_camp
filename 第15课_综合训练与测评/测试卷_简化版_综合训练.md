# 📝 计算机基础与程序设计综合测试卷

考试时间：60 分钟　　满分：100 分
**姓名**：********\_\_\_\_******** **得分**：********\_\_\_\_********

---

## 一、单选题（每题 4 分，共 60 分）

**1. 若 `int x = 5;`，则表达式 `x & (x - 1)` 的结果为（　　）**
A. 4
B. 5
C. 0
D. 1

**2. 在 C++ 中，如果需要通过函数内修改外部传入的变量本身，应采用哪种传参方式？（　　）**
A. 值传递
B. 引用（或指针）传递
C. 无法修改
D. 静态传递

**3. 定义结构体 `struct Node { int data; Node* next; };`，已知指针 `p` 指向某个节点，想要访问该节点中存储的数据，正确的写法是（　　）**
A. `p.data`
B. `p->data`
C. `*p.data`
D. `Node.data`

**4. 对于一个长度为 $N$ 的数组进行冒泡排序，在最坏情况下，需要进行的比较总次数为（　　）**
A. $N$
B. $N - 1$
C. $\frac{N(N-1)}{2}$
D. $N^2$

**5. 表达式 `! (4 >= 6 || 3 != 3)` 的逻辑真值是（　　）**
A. true
B. false
C. 1
D. 编译错误

**6. 将十进制数 27 转换为二进制数，结果是（　　）**
A. 11011
B. 11001
C. 10111
D. 11101

**7. 在一棵深度为 $k$的满二叉树中，总结点数最多有（　　）个。**
A. $2^k$
B. $2^{k-1}$
C. $2^k - 1$
D. $2^{k+1} - 1$

**8. 单向链表中，若要将指针 `p` 的当前节点从链表中断开并跳过它（假设 `pre` 是 `p` 的前驱节点），正确的操作是（　　）**
A. `pre->next = p;`
B. `p->next = pre;`
C. `pre->next = p->next;`
D. `p = p->next;`

**9. 执行以下代码段后，变量 `b` 的值为（　　）**
`int a = 10; int b = a >> 1; b = b ^ 1;`
A. 5
B. 4
C. 6
D. 2

**10. 设数组 `int arr[5] = {1, 3, 5, 7, 9};`，则 `arr[arr[1]]` 的值为（　　）**
A. 3
B. 5
C. 7
D. 9

**11. 关于全局变量和局部变量，下列说法正确的是（　　）**
A. 局部变量在整个程序运行期间都存在
B. 全局变量的默认初始值为 0
C. 局部变量不能和全局变量同名
D. 全局变量存放在栈区

**12. 若整型变量 `a = 7, b = 2`，则表达式 `(a % b) + (a / b)` 的值是（　　）**
A. 3
B. 4
C. 4.5
D. 5

**13. 关于 C++ 中的 `sizeof` 运算符，下列说法错误的是（　　）**
A. 它可以用于数据类型（如 `sizeof(int)`）
B. 它可以用于变量名（如 `sizeof(x)`）
C. 它的计算是在程序运行时（Runtime）执行的
D. 它的返回值通常表示所占用的内存字节数

**14. 在包含 $N$ 个元素的有序数组中，使用二分查找寻找一个不存在的元素，最坏情况下的时间复杂度为（　　）**
A. $O(1)$
B. $O(\log N)$
C. $O(N)$
D. $O(N \log N)$

**15. 下列哪一个标识符是合法的 C++ 变量名？（　　）**
A. `2nd_number`
B. `my-var`
C. `_index0`
D. `int`

---

## 二、判断题（每题 2 分，共 10 分）

**1.** 两个相同的整数进行按位异或（XOR）运算，其结果必定为 0。（　　）

**2.** 在 C++ 中，结构体（struct）只能包含数据变量，不能包含成员函数。（　　）

**3.** 二分查找算法既可以用于有序数组，也可以用于任何无序数组。（　　）

**4.** C++ 中通过指针传递数组参数时，实际上传递的是数组首元素的内存地址。（　　）

**5.** 逻辑运算表达式 `(A && B)` 中，如果 `A` 的值为 false，则程序根本不会进一步去求解 `B` 的真假，这称作“短路求值”。（　　）

---

## 三、完善程序题（每空 5 分，共 30 分）

### 第1题：插入排序算法实现

**题目描述**：给定一个包含 `n` 个整数的数组 `a`，请完善下面的插入排序算法，将数组按从小到大的顺序排列。

```cpp
#include <iostream>
using namespace std;

void insertionSort(int a[], int n) {
    for (int i = 1; i < n; i++) {
        int key = a[i];
        int j = i - 1;

        // 寻找元素 key 应该插入的位置，并将比 key 大的元素向后移动
        while (j >= 0 && a[j] > ______①) {
            a[______②] = a[j];
            j--;
        }

        // 放入正确的位置
        a[______③] = key;
    }
}

int main() {
    int arr[] = {5, 2, 4, 6, 1, 3};
    insertionSort(arr, 6);
    return 0;
}
```

### 第2题：求单链表中的最大值

**题目描述**：定义了一个存储整数的单向链表结构，请完善程序，遍历整个非空链表以找到所有节点中的最大数据值。

```cpp
#include <iostream>
using namespace std;

// 链表节点结构体定义
struct Node {
    int data;
    Node* next;
};

// 寻找链表中的最大元素
int findMax(Node* head) {
    if (head == NULL) {
        return -1; // 链表为空时返回特定值
    }

    int maxVal = ______①;    // 初始化最大值
    Node* current = head->next;

    // 遍历整个链表
    while (______②) {
        if (current->data > maxVal) {
            maxVal = current->data;
        }
        // 指针移向下一个节点
        current = ______③;
    }

    return maxVal;
}
```
