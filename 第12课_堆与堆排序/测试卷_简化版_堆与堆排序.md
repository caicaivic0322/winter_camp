# 📝 第12课 测试卷：堆与堆排序

**姓名**：____________________          **得分**：____________________

---

## 一、选择题（每题5分，共30分）

**1. 大顶堆的性质是（　　）**
A. 每个节点小于子节点　B. 每个节点大于等于子节点　C. 完全有序　D. 随机排列

**2. 在数组表示的堆中（从0开始），节点 i 的左子节点下标是（　　）**
A. 2i　　B. 2i+1　　C. (i-1)/2　　D. i+1

**3. 堆排序的时间复杂度是（　　）**
A. O(n)　　B. O(n log n)　　C. O(n²)　　D. O(log n)

**4. priority_queue 默认是（　　）**
A. 小顶堆　　B. 大顶堆　　C. 有序数组　　D. 无序数组

**5. 定义小顶堆应该写（　　）**
A. `priority_queue<int>`
B. `priority_queue<int, vector<int>, less<int>>`
C. `priority_queue<int, vector<int>, greater<int>>`
D. `queue<int>`

**6. 对于n个元素，取出最大的k个，使用堆的时间复杂度是（　　）**
A. O(k)　　B. O(n log k)　　C. O(n²)　　D. O(n)

---

## 二、判断题（每题5分，共20分）

**1. 堆顶元素一定是最大或最小值。（　　）**

**2. 堆可以用链表高效实现。（　　）**

**3. 堆排序的空间复杂度是O(1)。（　　）**

**4. priority_queue 的 pop() 返回堆顶元素。（　　）**

---

## 三、程序完善题（每题25分，共50分）

### 第1题：使用 priority_queue

```cpp
#include <iostream>
#include <queue>
using namespace std;

int main() {
    priority_queue<int> pq;
    pq.______①(5);
    pq.push(3);
    pq.push(8);

    cout << pq.______②() << endl;  // 输出最大值（堆顶）
    pq.______③();                   // 删除堆顶

    while (!pq.______④()) {
        cout << pq.top() << " ";
        pq.pop();
    }
    return 0;
}
```

### 第2题：下沉操作（建堆核心）

```cpp
// 维护大顶堆性质
void siftDown(int arr[], int i, int n) {
    while (2 * i + 1 < n) {
        int j = 2 * i + 1;  // 左子节点
        // 如果右子节点存在且更大
        if (j + 1 < n && arr[j + 1] ______① arr[j]) {
            j++;
        }

        // 如果父节点已经大于等于最大子节点
        if (arr[i] >= arr[j]) ______②;

        // 否则交换，继续下沉
        ______③(arr[i], arr[j]);
        i = ______④;
    }
}
```

---
