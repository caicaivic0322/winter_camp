# 📝 第13课 测试卷：堆与堆排序

**姓名**：********\_\_\_\_******** **得分**：********\_\_\_\_********

---

## 一、选择题（每题3分，共30分）

**1. 大顶堆的性质是（　　）**
A. 每个节点小于子节点　B. 每个节点大于等于子节点　C. 完全有序　D. 随机排列

**2. 在数组表示的堆中（从0开始），节点 i 的左子节点下标是（　　）**
A. 2i　　B. 2i+1　　C. (i-1)/2　　D. i+1

**3. 堆排序的时间复杂度是（　　）**
A. O(n)　　B. O(n log n)　　C. O(n²)　　D. O(log n)

**4. priority_queue 默认是（　　）**
A. 小顶堆　　B. 大顶堆　　C. 有序数组　　D. 无序数组

**5. 建堆的时间复杂度是（　　）**
A. O(n)　　B. O(n log n)　　C. O(n²)　　D. O(log n)

**6. 定义小顶堆应该写（　　）**
A. `priority_queue<int>`
B. `priority_queue<int, vector<int>, less<int>>`
C. `priority_queue<int, vector<int>, greater<int>>`
D. `queue<int>`

**7. 堆排序是（　　）**
A. 稳定排序　　B. 不稳定排序　　C. 半稳定　　D. 以上都不是

**8. 对于n个元素，取出最大的k个，使用堆的时间复杂度是（　　）**
A. O(k)　　B. O(n log k)　　C. O(n²)　　D. O(n)

**9. 堆是一种（　　）**
A. 完全二叉树　　B. 满二叉树　　C. 平衡二叉树　　D. 二叉搜索树

**10. 堆的应用不包括（　　）**
A. 优先队列　　B. Top K　　C. 二分查找　　D. 定时器

---

## 二、判断题（每题2分，共10分）

**1. 堆顶元素一定是最大或最小值。（　　）**

**2. 堆可以用链表高效实现。（　　）**

**3. 堆排序的空间复杂度是O(1)。（　　）**

**4. priority_queue 的 pop() 返回堆顶元素。（　　）**

**5. n个元素的堆，高度是 O(log n)。（　　）**

---

## 三、程序填空题（每空3分，共30分）

### 第1题：使用 priority_queue（15分）

```cpp
#include <iostream>
#include <queue>
using namespace std;

int main() {
    priority_queue<int> pq;
    pq.______①(5);
    pq.push(3);
    pq.push(8);

    cout << pq.______②() << endl;  // 输出最大值
    pq.______③();                   // 删除堆顶

    while (!pq.______④()) {
        cout << pq.top() << " ";
        pq.pop();
    }
    return 0;
}
```

### 第2题：下沉操作（15分）

```cpp
void siftDown(int arr[], int i, int n) {
    while (2 * i + 1 < n) {
        int j = ______①;  // 左子节点
        if (j + 1 < n && arr[j + 1] ______② arr[j]) j++;
        if (arr[i] >= arr[j]) ______③;
        ______④(arr[i], arr[j]);
        i = ______⑤;
    }
}
```

---

## 四、编程题（共30分）

### 第1题（15分）：堆排序

使用堆排序对数组进行升序排序。

### 第2题（15分）：找第K大

使用 priority_queue 找数组中第 K 大的元素。
