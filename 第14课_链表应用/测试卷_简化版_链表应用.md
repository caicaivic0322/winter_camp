# 📝 第14课 测试卷：链表应用

**姓名**：____________________          **得分**：____________________

---

## 一、选择题（每题5分，共30分）

**1. 反转链表使用迭代法，需要的指针变量个数最少是（　　）**
A. 1个　　B. 2个　　C. 3个　　D. 4个

**2. 快慢指针中，慢指针每次走1步，快指针每次走（　　）步**
A. 1　　B. 2　　C. 3　　D. 不确定

**3. 找链表中点时，快指针走到末尾，慢指针在（　　）**
A. 头部　　B. 中点　　C. 尾部　　D. 1/3处

**4. 合并两个有序链表的时间复杂度是（　　）**
A. O(1)　　B. O(m+n)　　C. O(m×n)　　D. O(nlogn)

**5. 判断链表有环使用的是（　　）**
A. 递归　　B. 快慢指针　　C. 栈　　D. 队列

**6. 链表反转后，原来的头节点变成（　　）**
A. 还是头　　B. 尾节点　　C. 中间节点　　D. 被删除

---

## 二、判断题（每题5分，共20分）

**1. 快慢指针相遇说明链表一定有环。（　　）**

**2. 反转链表只能用迭代实现。（　　）**

**3. 合并有序链表后，结果仍然有序。（　　）**

**4. 找链表倒数第k个节点需要遍历两次。（　　）**

---

## 三、程序完善题（每题25分，共50分）

### 第1题：反转链表

```cpp
#include <iostream>
using namespace std;

struct Node {
    int data;
    Node* next;
};

Node* reverse(Node* head) {
    Node* prev = ______①;
    Node* cur = head;

    while (cur != nullptr) {
        Node* next = cur->______②;  // 暂存后继节点
        cur->next = ______③;         // 修改指向
        prev = ______④;              // 更新 prev
        cur = ______⑤;               // 更新 cur
    }

    return prev;  // 返回新的头节点
}
```

### 第2题：判断有环

```cpp
bool hasCycle(Node* head) {
    Node* slow = head;
    Node* fast = head;

    while (fast && fast->______①) {
        slow = slow->next;          // 慢指针走一步
        fast = fast->______②->next; // 快指针走两步
        if (______③ == ______④) return true; // 相遇
    }

    return false;
}
```

---
