# 📝 第13课 测试卷：链表应用

**姓名**：******\_****** **得分**：******\_******

---

## 一、选择题（每题3分，共30分）

**1. 反转链表使用迭代法，需要的指针变量个数最少是（　　）**
A. 1个　　B. 2个　　C. 3个　　D. 4个

**2. 快慢指针中，慢指针每次走1步，快指针每次走（　　）步**
A. 1　　B. 2　　C. 3　　D. 不确定

**3. 找链表中点时，快指针走到末尾，慢指针在（　　）**
A. 头部　　B. 中点　　C. 尾部　　D. 1/3处

**4. 合并两个有序链表的时间复杂度是（　　）**
A. O(1)　　B. O(m+n)　　C. O(m×n)　　D. O(nlogn)

**5. 判断链表有环使用的是（　　）**
A. 递归　　B. 快慢指针　　C. 栈　　D. 队列

**6. 删除倒数第k个节点，快指针需要先走（　　）步**
A. k　　B. k-1　　C. k+1　　D. n-k

**7. 哨兵节点(dummy)的作用是（　　）**
A. 存储数据　　B. 简化边界处理　　C. 提高速度　　D. 节省空间

**8. 链表反转后，原来的头节点变成（　　）**
A. 还是头　　B. 尾节点　　C. 中间节点　　D. 被删除

**9. 静态链表使用（　　）实现**
A. 指针　　B. 数组　　C. 递归　　D. 栈

**10. 两个有环链表可能相交于（　　）**
A. 只有一点　　B. 多点　　C. 环上　　D. 以上都可能

---

## 二、判断题（每题2分，共10分）

**1. 快慢指针相遇说明链表一定有环。（　　）**

**2. 反转链表只能用迭代实现。（　　）**

**3. 合并有序链表后，结果仍然有序。（　　）**

**4. 静态链表比动态链表效率低。（　　）**

**5. 找链表倒数第k个节点需要遍历两次。（　　）**

---

## 三、程序填空题（每空3分，共30分）

### 第1题：反转链表（15分）

```cpp
Node* reverse(Node* head) {
    Node* prev = ______①;
    Node* cur = head;

    while (cur != nullptr) {
        Node* next = cur->______②;
        cur->next = ______③;
        prev = ______④;
        cur = ______⑤;
    }

    return prev;
}
```

### 第2题：判断有环（15分）

```cpp
bool hasCycle(Node* head) {
    Node* slow = ______①;
    Node* fast = head;

    while (fast && ______②) {
        slow = slow->next;
        fast = fast->______③->next;
        if (______④ == ______⑤) return true;
    }

    return false;
}
```

---

## 四、编程题（共30分）

### 第1题（15分）：合并两个有序链表

输入两个递增有序链表，输出合并后的有序链表。

### 第2题（15分）：删除链表的倒数第N个节点

给定链表和整数N，删除倒数第N个节点，返回头节点。

---

# 答案

## 一、选择题

1-5: C B B B B　　6-10: C B B B D

## 二、判断题

1-5: √ × √ × ×

## 三、填空题

### 第1题：① nullptr ② next ③ prev ④ cur ⑤ next

### 第2题：① head ② fast->next ③ next ④ slow ⑤ fast

## 四、编程题参考答案

### 第1题

```cpp
Node* mergeTwoLists(Node* l1, Node* l2) {
    Node dummy(0);
    Node* tail = &dummy;
    while (l1 && l2) {
        if (l1->data <= l2->data) {
            tail->next = l1; l1 = l1->next;
        } else {
            tail->next = l2; l2 = l2->next;
        }
        tail = tail->next;
    }
    tail->next = l1 ? l1 : l2;
    return dummy.next;
}
```

### 第2题

```cpp
Node* removeNthFromEnd(Node* head, int n) {
    Node dummy(0);
    dummy.next = head;
    Node *fast = &dummy, *slow = &dummy;
    for (int i = 0; i <= n; i++) fast = fast->next;
    while (fast) { fast = fast->next; slow = slow->next; }
    Node* temp = slow->next;
    slow->next = slow->next->next;
    delete temp;
    return dummy.next;
}
```
