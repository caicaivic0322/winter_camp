# 📝 第6课 测试卷：高精度乘法

**姓名**：____________________          **得分**：____________________

---

## 一、选择题（每题5分，共30分）

**1. 一个 50 位的大数与一个 30 位的大数相乘，结果最多有（　　）位**

A. 50 位
B. 80 位
C. 30 位
D. 1500 位

---

**2. 在大数乘大数的算法中，`c[i + j] += a[i] * b[j]` 这个公式表示（　　）**

A. a[i] 和 b[j] 相乘的结果存在第 i+j 位
B. a[i] 和 b[j] 相加的结果存在第 i+j 位
C. 进位处理
D. 去除前导零

---

**3. 计算 999 × 1 时，正确的结果是（　　）**

A. 999
B. 0
C. 1
D. 998

---

**4. 以下代码片段的作用是（　　）**

```cpp
if (num == "0" || x == 0) return "0";
```

A. 判断结果是否为负数
B. 处理乘以 0 的特殊情况
C. 去除前导零
D. 处理进位

---

**5. 大数 12345 存储为数组 `a = {5, 4, 3, 2, 1}`，则 `a[0] * 7` 的计算结果是（　　）**

A. 7
B. 35
C. 5
D. 12

---

**6. 高精度大数 × 小整数的时间复杂度是（　　）**

A. O(1)
B. O(n)
C. O(n²)
D. O(2^n)

---

## 二、判断题（每题5分，共20分）

**1. 高精度乘法中，如果其中一个乘数是 0，结果必须特判返回 "0"。（　　）**

---

**2. 两个 n 位数相乘，结果一定是 2n 位数。（　　）**

---

**3. 在大数乘大数中，每次 `a[i] * b[j]` 的结果最大是 81。（　　）**

---

**4. 高精度乘法的进位可以在每次相乘后立即处理，也可以最后统一处理。（　　）**

---

## 三、程序完善题（每题25分，共50分）

### 第1题：大数乘小整数

```cpp
#include <iostream>
#include <string>
#include <vector>
using namespace std;

string multiply(string num, int x) {
    if (x == 0) return ______①;

    vector<int> c;
    int carry = 0;

    for (int i = num.length() - 1; i >= 0 || ______②; i--) {
        int product = carry;
        if (i >= 0) {
            product += (num[i] - '0') * ______③;
        }
        c.push_back(product ______④ 10);
        carry = product / 10;
    }

    string result = "";
    for (int i = c.size() - 1; i >= 0; i--) {
        result += (char)(c[i] + ______⑤);
    }

    return result;
}

int main() {
    cout << multiply("123", 7) << endl;  // 应输出 861
    return 0;
}
```

---

### 第2题：大数乘大数

```cpp
#include <iostream>
#include <string>
#include <vector>
using namespace std;

string multiply(string num1, string num2) {
    if (num1 == "0" || num2 == "0") return "0";

    int len1 = num1.length();
    int len2 = num2.length();
    vector<int> c(len1 + len2, ______①);

    // 逐位相乘
    for (int i = len1 - 1; i >= 0; i--) {
        for (int j = len2 - 1; j >= 0; j--) {
            c[______②] += (num1[i] - '0') * (num2[j] - '0');
        }
    }

    // 处理进位
    for (int i = c.size() - 1; i > 0; i--) {
        c[i - 1] += c[i] / ______③;
        c[i] %= 10;
    }

    // 转字符串
    string result = "";
    bool leadingZero = ______④;
    for (int i = 0; i < c.size(); i++) {
        if (leadingZero && c[i] == 0) continue;
        leadingZero = ______⑤;
        result += (char)(c[i] + '0');
    }

    return result.empty() ? "0" : result;
}

int main() {
    cout << multiply("123", "45") << endl;  // 应输出 5535
    return 0;
}
```

---
