# 📝 第9课 测试卷：树与二叉树

**姓名**：******\_****** **得分**：******\_******

---

## 一、选择题（每题3分，共30分）

**1. 树的根节点的特点是（　　）**
A. 没有子节点　B. 没有父节点　C. 只有一个子节点　D. 是叶子节点

**2. 二叉树的每个节点最多有（　　）个子节点**
A. 1　　B. 2　　C. 3　　D. 无限

**3. 以下二叉树的前序遍历结果是（　　）**

```
    1
   / \
  2   3
```

A. 2,1,3　　B. 1,2,3　　C. 2,3,1　　D. 3,2,1

**4. 中序遍历的访问顺序是（　　）**
A. 根→左→右　　B. 左→根→右　　C. 左→右→根　　D. 右→根→左

**5. 在数组表示的完全二叉树中（下标从1开始），节点 i 的左子节点是（　　）**
A. i-1　　B. i+1　　C. 2\*i　　D. i/2

**6. 叶子节点的特点是（　　）**
A. 没有父节点　B. 没有子节点　C. 只有左子节点　D. 只有右子节点

**7. 归并排序的递归过程类似于（　　）遍历**
A. 前序　　B. 中序　　C. 后序　　D. 层序

**8. 快速排序的递归过程类似于（　　）遍历**
A. 前序　　B. 中序　　C. 后序　　D. 层序

**9. 以下代码实现的是（　　）遍历**

```cpp
void traverse(TreeNode* root) {
    if (!root) return;
    traverse(root->left);
    cout << root->val;
    traverse(root->right);
}
```

A. 前序　　B. 中序　　C. 后序　　D. 无法确定

**10. 满二叉树的特点是（　　）**
A. 只有一个节点　B. 每层都是满的　C. 只有叶子节点　D. 没有右子树

---

## 二、判断题（每题2分，共10分）

**1. 二叉树的左子树和右子树可以互换。（　　）**

**2. 前序遍历的第一个节点一定是根节点。（　　）**

**3. 后序遍历的最后一个节点一定是根节点。（　　）**

**4. 完全二叉树可以用数组高效存储。（　　）**

**5. 树的高度等于节点个数。（　　）**

---

## 三、程序填空题（每空3分，共30分）

### 第1题：前序遍历（15分）

```cpp
void preorder(TreeNode* root) {
    if (root == ______①) return;

    cout << root->______② << " ";  // 访问根
    preorder(root->______③);        // 遍历左子树
    preorder(root->______④);        // 遍历右子树
}
```

### 第2题：计算节点数（15分）

```cpp
int countNodes(TreeNode* root) {
    if (root == nullptr) return ______①;

    int leftCount = countNodes(root->______②);
    int rightCount = countNodes(root->______③);

    return ______④ + leftCount + rightCount;
}
```

---

## 四、编程题（共30分）

### 第1题（15分）：计算二叉树高度

编写函数 `int getHeight(TreeNode* root)` 计算二叉树的高度。

**示例**：

```
    1
   / \
  2   3
 / \
4   5
```

高度 = 3

### 第2题（15分）：统计叶子节点

编写函数 `int countLeaves(TreeNode* root)` 统计二叉树的叶子节点个数。

**示例**：上述二叉树的叶子节点是 4, 5, 3，共 3 个。

---

# 答案

## 一、选择题

1-5: B B B B C　　6-10: B C A B B

## 二、判断题

1-5: × √ √ √ ×

## 三、填空题

### 第1题：① nullptr ② val ③ left ④ right

### 第2题：① 0 ② left ③ right ④ 1

## 四、编程题参考答案

### 第1题

```cpp
int getHeight(TreeNode* root) {
    if (root == nullptr) return 0;
    return 1 + max(getHeight(root->left), getHeight(root->right));
}
```

### 第2题

```cpp
int countLeaves(TreeNode* root) {
    if (root == nullptr) return 0;
    if (root->left == nullptr && root->right == nullptr) return 1;
    return countLeaves(root->left) + countLeaves(root->right);
}
```
