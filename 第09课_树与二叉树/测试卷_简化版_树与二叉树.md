# 📝 第9课 测试卷：树与二叉树

**姓名**：********\_\_\_\_******** **得分**：********\_\_\_\_********

---

## 一、选择题（每题5分，共40分）

**1. 树的根节点的特点是（　　）**
A. 没有子节点　B. 没有父节点　C. 只有一个子节点　D. 是叶子节点

**2. 二叉树的每个节点最多有（　　）个子节点**
A. 1　　B. 2　　C. 3　　D. 无限

**3. 中序遍历的访问顺序是（　　）**
A. 根→左→右　　B. 左→根→右　　C. 左→右→根　　D. 右→根→左

**4. 叶子节点的特点是（　　）**
A. 没有父节点　B. 没有子节点　C. 只有左子节点　D. 只有右子节点

**5. 以下代码实现的是（　　）遍历**

```cpp
void traverse(TreeNode* root) {
    if (!root) return;
    traverse(root->left);
    cout << root->val;
    traverse(root->right);
}
```

A. 前序　　B. 中序　　C. 后序　　D. 无法确定

**6. 满二叉树的特点是（　　）**
A. 只有一个节点　B. 每层都是满的　C. 只有叶子节点　D. 没有右子树

**7. 斐波那契数列 `fib(n) = fib(n-1) + fib(n-2)` 的递归过程中，会产生大量的（　　）**
A. 重复计算
B. 内存泄漏
C. 死循环
D. 数组越界

**8. 汉诺塔问题中，将 n 个盘子从 A 移到 C，最少需要移动（　　）次**
A. n
B. n^2
C. 2^n - 1
D. 2n

---

## 二、判断题（每题5分，共20分）

**1. 二叉树的左子树和右子树可以互换。（　　）**

**2. 前序遍历的第一个节点一定是根节点。（　　）**

**3. 完全二叉树可以用数组高效存储。（　　）**

**4. 树的高度等于节点个数。（　　）**

---

## 三、程序完善题（共40分）

### 第1题：前序遍历（10分）

```cpp
#include <iostream>
using namespace std;

struct TreeNode {
    int val;
    TreeNode* left;
    TreeNode* right;
};

void preorder(TreeNode* root) {
    if (root == ______①) return;

    cout << root->______② << " ";  // 访问根
    preorder(root->______③);        // 遍历左子树
    preorder(root->______④);        // 遍历右子树
}
```

### 第2题：计算节点数（10分）

```cpp
int countNodes(TreeNode* root) {
    if (root == nullptr) return ______①;

    int leftCount = countNodes(root->______②);
    int rightCount = countNodes(root->______③);

    return ______④ + leftCount + rightCount;
}
```

### 第3题：汉诺塔递归（Hanoi）（20分）

```cpp
// 将 n 个盘子从 A 柱移到 C 柱，利用 B 柱
void hanoi(int n, char A, char B, char C) {
    if (n == 1) {
        cout << A << "->" << C << endl;
        return;
    }
    // 将 n-1 个盘子从 A 移到 B
    hanoi(______①, A, C, B);

    // 移动第 n 个盘子
    cout << A << "->" << C << endl;

    // 将 n-1 个盘子从 B 移到 C
    hanoi(n - 1, ______②, ______③, ______④);
}
```

---
