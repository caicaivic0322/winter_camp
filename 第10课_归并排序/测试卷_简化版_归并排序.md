# 📝 第10课 测试卷：归并排序

**姓名**：____________________          **得分**：____________________

---

## 一、选择题（每题5分，共30分）

**1. 归并排序采用的算法思想是（　　）**
A. 贪心　　B. 分治　　C. 动态规划　　D. 回溯

**2. 归并排序的时间复杂度是（　　）**
A. O(n)　　B. O(n log n)　　C. O(n²)　　D. O(log n)

**3. 归并排序需要的额外空间是（　　）**
A. O(1)　　B. O(log n)　　C. O(n)　　D. O(n²)

**4. 归并排序的核心操作是（　　）**
A. 交换相邻元素　B. 合并两个有序数组　C. 选择最小元素　D. 分区

**5. 归并排序是（　　）排序**
A. 不稳定　　B. 稳定　　C. 半稳定　　D. 无法确定

**6. 合并两个长度为 m 和 n 的有序数组，时间复杂度是（　　）**
A. O(m × n)　　B. O(m + n)　　C. O(max(m, n))　　D. O(1)

---

## 二、判断题（每题5分，共20分）

**1. 归并排序的时间复杂度在最好和最坏情况下都是 O(n log n)。（　　）**

**2. 归并排序是原地排序算法。（　　）**

**3. 归并排序是稳定排序。（　　）**

**4. 归并排序的递归深度是 log n。（　　）**

---

## 三、程序完善题（每题25分，共50分）

### 第1题：归并排序递归函数

```cpp
#include <iostream>
using namespace std;

void merge(int arr[], int l, int mid, int r);

void mergeSort(int arr[], int l, int r) {
    // 递归终点
    if (l ______① r) return;

    int mid = (l + r) / ______②;

    // 递归排序左半部分
    mergeSort(arr, l, ______③);
    // 递归排序右半部分
    mergeSort(arr, ______④, r);
    // 合并
    merge(arr, l, mid, r);
}
```

### 第2题：合并两个有序数组

```cpp
void merge(int arr[], int l, int mid, int r) {
    int temp[1010];
    int i = l, j = mid + 1, k = 0;

    // 比较并合并
    while (i <= mid && j <= r) {
        if (arr[i] <= arr[j]) {
            temp[k++] = arr[______①];
        } else {
            temp[k++] = arr[______②];
        }
    }

    // 处理剩余元素
    while (i <= mid) temp[k++] = arr[i++];
    while (j <= r) temp[k++] = arr[j++];

    // 拷贝回原数组
    for (int p = 0; p < k; p++) {
        arr[l + p] = ______③;
    }
}
```

---
